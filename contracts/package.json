{
  "name": "contracts",
  "version": "1.0.0",
  "description": "DrivetoSurvive Smart Contracts",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.ts",
    "deploy:testnet": "hardhat run scripts/deploy.ts --network hedera-testnet",
    "deploy:all": "hardhat run scripts/deploy-all.ts",
    "deploy:all:testnet": "hardhat run scripts/deploy-all.ts --network hedera-testnet",
    "clean": "hardhat clean"
  },
  "dependencies": {
    "@hashgraph/sdk": "^2.49.2",
    "@openzeppelin/contracts": "^5.0.0",
    "@pythnetwork/pyth-sdk-solidity": "^4.2.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@typechain/ethers-v6": "^0.5.0",
    "@typechain/hardhat": "^9.0.0",
    "@types/node": "^20.0.0",
    "dotenv": "^17.2.3",
    "hardhat": "^2.22.0",
    "ts-node": "^10.9.0",
    "typechain": "^8.3.0",
    "typescript": "^5.0.0"
  }
}
